# Bowling Game

This project contains a ten-frame bowling score calculator and 16 unit tests.

## Run the examples

```bash
python3 example_usage.py
```

## Run all tests

```bash
python3 -m unittest test_bowling -v
```

The verbose test command displays each test and should finish with:

```text
Ran 16 tests
OK
```